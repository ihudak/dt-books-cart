## Spring Datasource (Datasource configuration)
spring.datasource.url=jdbc:mysql://${DT_DB_SERVER:localhost}:${DT_DB_PORT:3306}/${DT_DB_NAME:dt_books_cart}?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${DT_DB_USER:user}
spring.datasource.password=${DT_DB_PASS:p@ssw0rD!}

# Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=update

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

server.port=${DT_SERVER_PORT:8083}

http.service.clients=${DT_CLIENTS_SERVER_URL:http://localhost:8081/api/v1/clients}
http.service.books=${DT_BOOKS_SERVER_URL:http://localhost:8082/api/v1/books}
